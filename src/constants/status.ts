export enum RESPONSE_STATUS {
    NOT_ALLOWED = 'not allowed',
    NOT_FOUND = 'not found',
    AUTHENTICATION_FAILED = 'authentication failed',
    INVALID_CREDENTIALS = 'invalid crendetials',
    UNAUTHORIZED = 'unauthorized',
    SUCCESS = 'success'
}